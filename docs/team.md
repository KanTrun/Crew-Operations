# Đội NHỊP QUÁN — map GitHub

> Cập nhật handle thật rồi sửa `.github/CODEOWNERS`. Hiện chỉ có `@KanTrun` trên remote nên mọi vùng tạm trỏ về account này.

| Vai trò hồ sơ | Trách nhiệm | GitHub | Trạng thái |
|---------------|-------------|--------|------------|
| **A** | Solver, gates, opsengine, playbook | `@KanTrun` (tạm) | Cần handle riêng |
| **B** | API, orchestration, CI, infra | `@KanTrun` | Lead / owner repo |
| **C** | Agents, router, eval, messaging | `@KanTrun` (tạm) | Cần handle riêng |
| **D** | Web PWA, templates YAML, docs trình bày | `@KanTrun` (tạm) | Cần handle riêng |

## Khi có đủ 4 account

1. Mời collaborator trên GitHub (Write)  
2. Sửa bảng trên + `.github/CODEOWNERS`  
3. Bật branch protection `main`: require PR, require CODEOWNERS review, require status checks, no force-push  
4. Mỗi người chỉ mở nhánh đúng tiền tố vùng của mình  

## Liên hệ vận hành

- Repo: https://github.com/KanTrun/CA-CONG-BANG  
- Plan active: `plans/260821-2221-nhip-quan-lot1-full-delivery/`
