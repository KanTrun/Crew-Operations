# Python
__pycache__/
*.py[cod]
.venv/
.venv*/
venv/

.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/
dist/
build/

# Node
node_modules/
apps/web/.next/
apps/web/out/
apps/web/test-results/
apps/web/playwright-report/
apps/web/tsconfig.tsbuildinfo
npm-debug.log*

# Env / secrets
.env
.env.*
!.env.example

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Coverage
.coverage
.coverage.*
htmlcov/

# Local test/debug artifacts (không commit)
_*.txt
_*.log

# AgentKit / Kiro — công cụ trợ lý cục bộ, không phải mã sản phẩm.
# .kiro/agentkit chứa bản export portable (1491 tệp) nên tuyệt đối không commit.
.kiro/

# Docker
*.log

# Generated solver output (CI regenerates via make bench)
data/out/lich_tuan.json
data/out/so_lan_sua.jsonl
data/out/cam_nang.json
data/out/audit.jsonl
data/out/msg_sent.jsonl
data/quan.db
data/quan.db-wal
data/quan.db-shm
# Backup DB tự sinh bởi `seed_professional_fixture.py --reset` (backup_db()) —
# artifact của từng lần chạy, không cho tích tụ binary vào repo.
# Hai backup có chủ đích vẫn track: `quan-pre-phase0.db` (có sẵn) và
# `quan-pre-gd10-260913.db` (bằng chứng GĐ10 trong bao-cao.md).
data/backups/quan-pre-reset-*.db
# Coverage JSON do `pytest --cov-report=json` sinh ra — hàng trăm KB và dễ lỗi
# thời; con số thật đã nằm trong log `.log` cùng thư mục.
plans/*/coverage*.json
data/test_*.json
data/full_benchmark_report.json
*.mp3
!data/out/.gitkeep
data/uploads/

# Runtime data của apps/api (db, log mail, upload) — sinh ra khi chạy local, không commit
apps/api/data/

.claude/


.env*
