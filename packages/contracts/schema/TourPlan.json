{
  "$defs": {
    "TourStep": {
      "description": "Một bước tour — anchor deterministic + narration có nguồn.",
      "properties": {
        "step_id": {
          "minLength": 1,
          "title": "Step Id",
          "type": "string"
        },
        "anchor_id": {
          "minLength": 1,
          "title": "Anchor Id",
          "type": "string"
        },
        "narrative": {
          "minLength": 1,
          "title": "Narrative",
          "type": "string"
        },
        "citation_memory_ids": {
          "items": {
            "type": "string"
          },
          "title": "Citation Memory Ids",
          "type": "array"
        }
      },
      "required": [
        "step_id",
        "anchor_id",
        "narrative"
      ],
      "title": "TourStep",
      "type": "object"
    }
  },
  "description": "Tour planner: chuỗi anchor đóng (không LLM thêm sự kiện chưa có nguồn).",
  "properties": {
    "tour_id": {
      "minLength": 1,
      "title": "Tour Id",
      "type": "string"
    },
    "steps": {
      "items": {
        "$ref": "#/$defs/TourStep"
      },
      "minItems": 1,
      "title": "Steps",
      "type": "array"
    },
    "grounded": {
      "default": true,
      "title": "Grounded",
      "type": "boolean"
    }
  },
  "required": [
    "tour_id",
    "steps"
  ],
  "title": "TourPlan",
  "type": "object"
}