[project]
name = "ca-contracts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.9.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ca_contracts"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
