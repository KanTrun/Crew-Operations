[project]
name = "ca-playbook"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ca_playbook"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
