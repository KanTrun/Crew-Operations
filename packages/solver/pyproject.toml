[project]
name = "ca-solver"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "ortools>=9.10",
    "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ca_solver"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
