name: Build & push images to GHCR

on:
  push:
    branches: [main]
    paths:
      - "apps/**"
      - "packages/**"
      - "infra/docker/**"
      - ".github/workflows/docker-ghcr.yml"

  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        include:
          - name: nhipquan-api
            dockerfile: infra/docker/Dockerfile.api

          - name: nhipquan-web
            dockerfile: infra/docker/Dockerfile.web

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Web build arg — API URL public
        if: matrix.name == 'nhipquan-web'
        run: echo "WEB_API_URL=https://nhipquan.duckdns.org" >> $GITHUB_ENV

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push ${{ matrix.name }}
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            ${{ matrix.name == 'nhipquan-web' && format('NEXT_PUBLIC_API_URL={0}', env.WEB_API_URL) || '' }}
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/crew-operations/${{ matrix.name }}:latest
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/crew-operations/${{ matrix.name }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
